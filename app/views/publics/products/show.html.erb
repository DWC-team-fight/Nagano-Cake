<%= image_tag @product.image, size: "340x220", fallback: "no_image.jpg" %>
<h3><%= @product.name %></h3>
   <p><%= @product.description %></p>
   <p>価格: <%= @product.tax_excluded_price %>円（税抜き）</p>
   <% if @product.is_status == true %>
   <h4>¥<%= (@product.price * 1.08).to_i.to_s(:selimited, delimiter: ',') %></h4>
   <p>(税込)</p>
   <%= form_with model:@cart_item, url: cart_items_path, method: :post do |f| %>
     <%= f.hidden_field :product_id, value: @product.id %>
     <%= f.select :count,(1..10).to_a, include_blank: "個数選択" %>
     <%= f.submit "カートに入れる", class: "btn btn-success" %>
   <% end %>
   <% else %>
   <h4>売り切れ</h4>
   <% end %>

